FROM python:3.12-slim

WORKDIR /lab

RUN apt-get update && \
    apt-get install -y --no-install-recommends \
    sudo \
    vim \
    libtk8.6 \
    tk8.6 \
    libtcl8.6 \
    curl && \
    rm -rf /var/lib/apt/lists/*

CMD ["/bin/bash"]
